using System.Collections.Generic;
using UnityEngine;

public class Recipe : MonoBehaviour
{
    /*
     * Lets say each menu item has a recipe associated with it.
     * A recipe can be simple or complex, depending on the item.
     * A simple recipe requires just a few ingredients and readily available tools.
     * A complex recipe requires more ingredients and specialized tools (can be bought and unlocked later in the game).
     */

    public Recipe(MainDish)
    {
        
    }
}
